export function PowerA234(A: number) {
    const A2 = Math.pow(A, 2);
    const A3 = Math.pow(A, 3);
    const A4 = Math.pow(A, 4);
    return "Power 2: " + A2 + " Power 3: " + A3 + " Power 4: " + A4;
}